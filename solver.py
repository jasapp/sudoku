# experimental methods of solving ./puzzles.py with ./sudoku.py
from sudoku import Board
from puzzles import p1, p2, p3
from templates import empty, idx, invalid

def solve_it_like_a_real_human_bean() -> int:
    # sb1 = Board(p1)
    # sb2 = Board(p2)
    idx_board = Board(idx)

solve_it_like_a_real_human_bean()
